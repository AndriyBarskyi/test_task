CREATE TABLE IF NOT EXISTS `test_task`.`number` (
    `id` BIGINT NOT NULL ,
    `number_value` BIGINT NOT NULL,
    PRIMARY KEY (`id`)
);

--INSERT INTO `number` (`id`, `number_value`) VALUES (1, 1);